# resources Submodule (src)

Contains JavaFX and other resources for the GUI.

- `css/` — Stylesheets for the dashboard
- `fxml/` — FXML layout files
- `images/` — Icons and images for the GUI

These resources are loaded by the GUI at runtime.
