# css (resources)

Contains CSS stylesheets for the JavaFX dashboard.

- `dashboard.css` — Main dashboard styling
- `styles.css` — Additional styles

These files control the look and feel of the GUI.
