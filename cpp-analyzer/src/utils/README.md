# utils Submodule (src)

Contains utility classes for the analyzer system.

- `ReportEntry.java` — Helper for report generation and formatting.

These classes support the main analyzer and GUI logic.
