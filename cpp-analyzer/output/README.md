# output Directory

This folder contains static analysis reports generated by the analyzers.

- Each file is named after the analyzed source file (e.g., `cpp-analysis-report-test.cpp.txt`).
- Reports are generated automatically when you run the analyzers or use the GUI/server.

You do not need to edit or commit these files.
