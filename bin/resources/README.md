# bin/resources

Contains runtime resources for the GUI (CSS, FXML, images).

- `css/` — Stylesheets
- `fxml/` — Layout files
- `images/` — Icons and images

These are loaded by the JavaFX GUI at runtime.
